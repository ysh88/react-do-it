import React from 'react';

const ModalContext1 = React.createContext();
const ModalContext2 = React.createContext();
const { Consumer } = ModalContext2;
export { ModalContext1, ModalContext2, Consumer as Consumer2 };
